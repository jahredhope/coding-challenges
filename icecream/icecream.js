function getIcecreams(money, values) {
  var length = 0;
  var potentials
  function insertInOrder(value) {

  }
  function binarySearch(value, pos) {
    var minIndex = 0;
    var maxIndex = values.length-1;
    var currentIndex, currentElement;

    while(minIndex <= maxIndex) {
      currentIndex = (maxIndex + minIndex) >> 1;
    }
  }
  return [4, 1];
}


module.exports = {getIcecreams};
